subroutine test_truthyness(a: uint64, b: uint64, c: string, d: bytes, e: uint64): void
{
  assert(True)
  assert(True)
  assert(!0)
  assert(1)
  assert(!bitlen(0))
  assert(Boolean(bitlen(1)))
  assert(!len(0x))
  assert(Boolean(len("abc")))
  assert(!len(""))
  assert(Boolean(len("abc")))
  assert(!False)
  assert(True)
}
subroutine test_booleans_are_equal(): void
{
  assert(!!True == !!True)
  assert(!!1 == !!5)
  assert(!!Boolean(bitlen(1)) == !!Boolean(bitlen(5)))
  assert("abc" != 0x == "abcdef" != 0x)
  assert("abc" != 0x == "abcdef" != 0x)
}