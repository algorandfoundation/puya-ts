subroutine test(x: group_transaction_1): void
{
  assert(gtxns<CloseRemainderTo>(x) == global<ZeroAddress>() and gtxns<Receiver>(x) == global<CurrentApplicationAddress>() and gtxns<Sender>(x) == txn<Sender>())
}