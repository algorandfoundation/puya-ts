contract EnsureBudgetContract
{
  approvalProgram(): bool
  {
    ensure_budget(100, 0)
    ensure_budget(100, 0)
    ensure_budget(100, 1)
    ensure_budget(100, 2)
    return True
  }
  
  clearProgram(): bool
  {
    return True
  }
  
}