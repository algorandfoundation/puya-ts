subroutine test_for_of_loop(items: readonlytuple[uint64, uint64, uint64]): uint64
{
  total: uint64 = 0
  for (item in items) {
    total: uint64 = total + item
    #loop₁ᶜ:
  }
  #loop₁ᵇ:
  return total
}