subroutine test(x: uint64, y: uint64): bool
{
  var a: uint64 = (x < 4 ? x : y) + x
  var b: uint64 = (Boolean(y % 2) ? 2 : 1)
  var c: uint64 = (x > y ? (y > 10 ? 3 : y) : x)
  var d: uint64 = (Boolean(y % 2) ? 2 : 1) + (x < 4 ? x : y)
  var e: uint64 = (Boolean(x) ? x : 4)
  var f: bool = (Boolean(y % 2) ? 2 : 1) == (x < 4 ? x : y)
  return True
}