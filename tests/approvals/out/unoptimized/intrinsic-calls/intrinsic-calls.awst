subroutine test(a: uint64, b: bytes): void
{
  x: uint64 = ($0 = setbit(a, 8, 1))
  y: bytes = ($1 = setbit(b, 12, 9))
  z: bytes[8] = ($2 = itob(x))
  g: bytes[32] = ($3 = sha256(reinterpret_cast<bytes>(z)))
}