contract MyContract
{
  approvalProgram(): bool
  {
    return arc4Router()
  }
  
  clearProgram(): bool
  {
    return True
  }
  
  test(): void
  {
    a: bytes[8] = ~bzero(8)
    b: uint64 = btoi(reinterpret_cast<bytes>(a))
    assert(b == 18446744073709551615)
    c: uint64 = shr(b, 32)
    assert(c == 4294967295)
    assert(gtxns<ApplicationID>(txn<GroupIndex>()) == txn<ApplicationID>())
    assert(txnas<ApplicationArgs>(0) == reinterpret_cast<bytes>(Method("test()void")))
    assert(gtxnsas<ApplicationArgs>(txn<GroupIndex>(), 0) == reinterpret_cast<bytes>(Method(name="test",args=(),result=void,resource_encoding="value")))
  }
  
  test2(size: uint64): void
  {
    a: bytes = bzero(size)
    assert(len(a) == size)
  }
  
  __algots__.defaultCreate(): void
  {
  }
  
}