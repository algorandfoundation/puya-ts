subroutine test(x: uint64, y: uint64): bool
{
  a: uint64 = (x < 4 ? x : y) + x
  b: uint64 = (Boolean(y % 2) ? 2 : 1)
  c: uint64 = (x > y ? (y > 10 ? 3 : y) : x)
  d: uint64 = (Boolean(y % 2) ? 2 : 1) + (x < 4 ? x : y)
  e: uint64 = (Boolean(x) ? x : 4)
  f: bool = (Boolean(y % 2) ? 2 : 1) == (x < 4 ? x : y)
  return True
}