subroutine test(x: uint64, y: uint64): bool
{
  a: uint64 = ($0 = (x < 4 ? x : y) + x)
  b: uint64 = ($1 = (Boolean(y % 2) ? 2 : 1))
  c: uint64 = ($2 = (x > y ? (y > 10 ? 3 : y) : x))
  d: uint64 = ($3 = (Boolean(y % 2) ? 2 : 1) + (x < 4 ? x : y))
  e: uint64 = ($4 = (Boolean(x) ? x : 4))
  f: bool = ($5 = (Boolean(y % 2) ? 2 : 1) == (x < 4 ? x : y))
  return True
}