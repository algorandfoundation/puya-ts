contract MutableArraysAlgo
{
  approvalProgram(): bool
  {
    return arc4Router()
  }
  
  clearProgram(): bool
  {
    return True
  }
  
  test(length: uint64): void
  {
    ma: WArray = new WArray()
    i: uint64 = 0
    while (i < length) {
      ma.push(...<tuple>[i]
      #loop₁ᶜ:
      i++
    }
    #loop₁ᵇ:
    assert(ma.length == length)
    assert(ma.pop() == length - 1)
  }
  
  __algots__.defaultCreate(): void
  {
  }
  
}