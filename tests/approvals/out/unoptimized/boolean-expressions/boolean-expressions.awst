subroutine test(a: uint64, b: uint64, c: string, d: bytes, e: uint64): bool
{
  x: uint64 = ($0 = (Boolean(($1 = (Boolean(a) ? a : b))) ? $1 : e))
  assert(Boolean((Boolean(($2 = (Boolean(a) ? b : a))) ? e : $2)), comment=a or b)
  y: uint64 = ($3 = (Boolean(a) ? b : a))
  z: uint64 = ($4 = (Boolean(a) and Boolean(len(d)) or Boolean(y) ? (Boolean(x) ? x : y) : (Boolean(x) ? y : x)))
  if (Boolean(a) or Boolean(len(c)) and Boolean(len(d))) {
    return True
  }
  f: uint64 = 0
  if (Boolean((f = ($5 = (Boolean(a) ? a : b))))) {
    return True
  }
  return False
}