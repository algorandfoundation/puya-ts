contract Greeter
{
  approvalProgram(): bool
  {
    return arc4Router()
  }
  
  clearProgram(): bool
  {
    return True
  }
  
  createApplication(): void
  {
  }
  
  greet(name: string): string
  {
    return "Hello, " + name + "!"
  }
  
  greetTwo(firstName: string, lastName: string): string
  {
    return "Hello, " + firstName + " and " + lastName + "!"
  }
  
}