{
  "GHSA-v2hh-gcrm-f6hx": {
    "active": true,
    "notes": "fast-uri host-confusion (backslash authority delimiter). Only reachable via dev/build tooling, not parsing untrusted URLs in a security-sensitive path. Patched in fast-uri 3.1.4, but the lockfile resolves 3.1.3; remove this exception once the tree carries >=3.1.4.",
    "expiry": "2026-10-31"
  }
}
